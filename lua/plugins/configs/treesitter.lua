return {
    ensure_installed = { "c", "css", "javascript", "lua", "typescript", "svelte", "zig" },
    sync_install = true,

    auto_install = true,

    highlight = {
        enable = true,
        additional_vim_regex_highlighting = false,
    },
    indent = { enable = true },
}
